/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['atbipbhagnbuqiuntcbb.supabase.co', 'another-domain.com'],
  },
};

export default nextConfig;
